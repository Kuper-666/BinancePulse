name: Build and Release

on:
  push:
    branches: [ "master" ]
    paths-ignore:
      - 'BinancePulse/BinancePulse.csproj'   # Не запускать при изменении .csproj (коммит версии)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Skip if version bump commit
        id: skip
        run: |
          if git log -1 --pretty=%B | grep -q "Bump version to"; then
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi
        shell: bash

      - name: Setup .NET
        if: steps.skip.outputs.skip == 'false'
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Get and increment version
        if: steps.skip.outputs.skip == 'false'
        id: get_version
        shell: pwsh
        run: |
          $csproj = "BinancePulse/BinancePulse.csproj"
          [xml]$xml = Get-Content $csproj
          $current = $xml.Project.PropertyGroup.VersionPrefix
          if (-not $current) { $current = "1.0.0" }
          $parts = $current -split '\.'
          $newVersion = "$($parts[0]).$($parts[1]).$([int]$parts[2] + 1)"
          Write-Host "New version: $newVersion"
          $xml.Project.PropertyGroup.VersionPrefix = $newVersion
          $xml.Save($csproj)
          echo "VERSION=$newVersion" >> $env:GITHUB_OUTPUT

      - name: Commit version bump
        if: steps.skip.outputs.skip == 'false'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add BinancePulse/BinancePulse.csproj
          git commit -m "Bump version to ${{ steps.get_version.outputs.VERSION }}" || echo "No changes"
          git push

      - name: Restore
        if: steps.skip.outputs.skip == 'false'
        run: dotnet restore BinancePulse/BinancePulse.csproj

      - name: Build
        if: steps.skip.outputs.skip == 'false'
        run: dotnet build BinancePulse/BinancePulse.csproj -c Release --no-restore

      - name: Publish (single file)
        if: steps.skip.outputs.skip == 'false'
        run: dotnet publish BinancePulse/BinancePulse.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:DebugType=none -p:DebugSymbols=false -o ./publish

      - name: Create ZIP archive
        if: steps.skip.outputs.skip == 'false'
        run: Compress-Archive -Path .\publish\* -DestinationPath BinancePulse_v${{ steps.get_version.outputs.VERSION }}.zip

      # ===== НОВЫЙ ШАГ: Установка Inno Setup =====
      - name: Install Inno Setup
        if: steps.skip.outputs.skip == 'false'
        run: choco install innosetup -y
        shell: powershell

      # ===== НОВЫЙ ШАГ: Сборка установщика =====
      - name: Build installer
        if: steps.skip.outputs.skip == 'false'
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "installer.iss"

      - name: Create Release
        if: steps.skip.outputs.skip == 'false'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.get_version.outputs.VERSION }}
          name: Release v${{ steps.get_version.outputs.VERSION }}
          files: |
            BinancePulse_v${{ steps.get_version.outputs.VERSION }}.zip
            Installers/BinancePulse_Setup.exe
          generate_release_notes: true
          # Раскомментируйте следующую строку, если нужен предрелиз:
          # prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
